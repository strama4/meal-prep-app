{
  "name": "meal-prep-app-backend",
  "version": "1.0.0",
  "description": "Meal prep app",
  "scripts": {
    "client": "cd client && npm run start",
    "server": "cd backend/src && nodemon server.js",
    "dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Luke Strama",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "jasmine": "^3.3.0",
    "nodemon": "^1.18.6",
    "request": "^2.88.0"
  },
  "devDependencies": {
    "concurrently": "^4.0.1"
  }
}
